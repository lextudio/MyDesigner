<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MyDesigner.XamlDesigner.ViewModels.Tools"
             xmlns:services="clr-namespace:MyDesigner.Designer.Services;assembly=MyDesigner.Designer"
             x:Class="MyDesigner.XamlDesigner.Views.Tools.ErrorListToolView"
             x:DataType="vm:ErrorListToolViewModel">
    
    <ListBox x:Name="uxErrorListBox"
             Background="Transparent"
             BorderThickness="0">
        <ListBox.ItemTemplate>
            <DataTemplate x:DataType="services:XamlError">
                <StackPanel Orientation="Horizontal" Margin="2">
                    <TextBlock Text="{Binding Line}" 
                               Width="40" 
                               Foreground="Blue" 
                               FontWeight="Bold"/>
                    <TextBlock Text=" / " Foreground="Gray"/>
                    <TextBlock Text="{Binding Column}" 
                               Width="40" 
                               Foreground="Blue"/>
                    <TextBlock Text=" - " Foreground="Gray"/>
                    <TextBlock Text="{Binding Message}" 
                               TextWrapping="Wrap"
                               Foreground="Red"/>
                </StackPanel>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
    
</UserControl>